# Requires Python 3.12 (kokoro 0.9.x pins <3.13). install.sh pins this via
# `uv venv --python 3.12`; install with `uv pip install -r requirements.txt`.
#
# sentencepiece + pypinyin are required by sherpa-onnx's text2token (the
# open-vocab wake-word tokenizer). sherpa-onnx imports them lazily and does NOT
# declare them as dependencies, so they must be listed explicitly.
#
# NOTE: kokoro's G2P (misaki) also needs spacy's en_core_web_sm model, which is
# NOT on PyPI — install.sh fetches it from GitHub releases separately.
sounddevice
numpy
faster-whisper
sherpa-onnx
sentencepiece
pypinyin
kokoro>=0.9.4
soundfile
edge-tts
