# Requires Python 3.12 (kokoro 0.9.x pins <3.13). install.sh pins this via
# `uv venv --python 3.12`; install with `uv pip install -r requirements.txt`.
# sentencepiece is required by sherpa-onnx's text2token (open-vocab wake word) —
# sherpa-onnx doesn't declare it, so it must be listed explicitly.
sounddevice
numpy
faster-whisper
sherpa-onnx
sentencepiece
kokoro>=0.9.4
soundfile
edge-tts
